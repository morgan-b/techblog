{{#each blogs as |blog|}}
<div class="row mb-4 blog">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      <a href="/blog/{{blog.id}}">{{blog.title}}</a>
    </h2>
    <p>Created by {{blog.user.username}} on {{format_time blog.date_created}}</p>
  </div>
</div>
{{/each}}
